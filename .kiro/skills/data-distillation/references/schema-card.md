Copy this file to `.kiro/specs/<kebab-name>/cards/schema-card.md`. The card holds one section
per file type; repeat the section below for each file type, so each type has its own File
list, Columns table, and Fixture list. `scripts/distill-fixture.ps1` drafts the column index,
type, shape, and numeric range; the operator fills the rest and chooses a generic name for
every column by hand. Delete these instruction lines when done.

# Schema card

## <File role, for example "weekly instrument export">

### File

- Filename pattern: `<generic pattern, for example export_<YYYYMMDD>.txt>`
- Encoding: <UTF-8, UTF-16 LE with BOM, Windows-1252>
- Line ending: <CRLF, LF>
- Delimiter: <tab, comma, semicolon, fixed width with column positions>
- Quote character: <none, double quote>
- Header rows: <count, and what each header row holds>
- Data rows: <typical count or range>
- Trailing block: <none, or "a summary block of <n> lines starting with <marker>">
- Produced by: <the system or tool, by role, not by product name if that is sensitive>
- Consumed by: <the script or step in the Mechanism section>

### Columns

| # | Generic name | Type | Unit or format | Nullable | Notes |
| --- | --- | --- | --- | --- | --- |
| 1 | <timestamp> | <datetime> | <yyyy-MM-dd HH:mm:ss> | <no> | <first column; local time> |
| 2 | <channel_id> | <string> | <letters and digits, length 6> | <no> | <renamed from a sensitive name> |
| 3 | <reading> | <decimal> | <unit> | <yes> | <blank when the sensor is off> |
| 4 | <status> | <enum> | <one of OK, WARN, FAIL> | <no> | |

### Fixture

- Fixture file: `fixtures/<generic-name>.<ext>`
- Rows: <20>
- Generated by: <scripts/distill-fixture.ps1 on <date>, or "by hand">
- Reviewed by: <the operator, on <date>>

### Classification

- Checklist run: <yes, by the operator, on <date>>
- Renamed columns: <list the generic names that replaced sensitive ones; do not list the
  originals>
